工厂模式的目的就是对象的创建过程的封装，虽然工厂方法不一定每次都返回一个新的对象，
但工厂方法都应当返回在工厂角色中被实例化的对象
工厂方法:
    1。每类工厂生产一类产品
    角色: 抽象产品，具体产品实现， 抽象工厂，具体工厂实现，工厂调用者

抽象工厂：
    2。 每个工厂生产N多类产品，

    角色：抽象产品，具体产品实现， 抽象工厂，具体工厂实现，工厂调用者

    如果抽象工厂需要添加一类产品，所有的具体实现工厂都要实现这个新增的产品

    产品族的扩展将是一件十分费力的事情，假如产品族中需要增加一个新的产品，则几乎所有的工厂类都需要进行修改。
    所以使用抽象工厂模式时，对产品等级结构的划分是非常重要的


